import { HistoricalExchangeRate } from './historical-exchange-rate';

describe('ExchangeRate', () => {
  it('should create an instance', () => {
    expect(new HistoricalExchangeRate()).toBeTruthy();
  });
});
